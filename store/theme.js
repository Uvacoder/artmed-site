export const state = () => ({
  config: { psiMode: false, darkMode: false }
})

export const mutations = {
  SET_CONFIG (state, config) {
    state.config = config
  }
}

export const actions = {
}

export const getters = {
}
